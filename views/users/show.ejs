<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Creepster&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/app.css" />
    <title>Monstr - Evil Profile</title>
  </head>
  <body>
    <h1>Monster Profile - <%= user.fname %> <%= user.lname %></h1>
    <div class="actions">
      <a class="buttonize" href="/auth/signup">Sign Up</a>
      <a class="buttonize" href="/auth/login">Log In</a>
      <a class="buttonize" href="/posts">News Feed</a>
      <a class="buttonize" href="/posts/new">New Post</a>
      <a class="buttonize" href="/users">See All Users</a>
    </div>
    <br />
    <h2><%= user.description %></h2>
    <img class="profile" src="<%= user.profilePic %>" /><br />
    <h2>Posts:</h2>
    <% for (let i = 0; i < user.Posts.length; i++){ %>
    <ul>
      <li><%=user.Posts[i].textContent%></li>
    </ul>
    <br />
    <img src="<%=user.Posts[i].picture%>" />
    <br />
    <% } %>

    <br /><br />
    <a href="/users/edit/<%=user.id%>">Edit</a>
    <a href="/users">Back to Main</a>
  </body>
</html>
